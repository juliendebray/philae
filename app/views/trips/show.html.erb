<div class="container-fluid">
  <div class="row">
    <div class="col-md-3 hidden-xs trip-home-sidebar">
      <%= link_to current_user do %>
        <div class="user-info text-center">
          <div class="user-picture">
            <%= image_tag(current_user.picture, class:'img-circle') %>
          </div>
          <div class="user-content">
            <div class="user-name">
              <%= current_user.name %>
            </div>
            <div class="user-trips">
              <%= current_user.trips.count %> voyages
            </div>
          </div>
        </div>
      <% end %>
      <div class="vertical-spacer-xxs"></div>
      <div class="home-trip-menu">
        <ul class="list-unstyled">
          <li class='text-indent-35'>
            <em><small>Que souhaites-tu faire?</small></em>
          </li>
          <li>
            <i class="fa fa-tachometer"></i>  Tableau de bord
          </li>
          <li>
            <i class="fa fa-lightbulb-o"></i>  Trouver de l'inspiration
          </li>
          <li>
            <i class="fa fa-map-marker"></i>  Ajouter des expériences
          </li>
          <li>
            <i class="fa fa-comment"></i>  Demander des conseils
          </li>
          <li>
            <i class="fa fa-cloud-download"></i>  Finaliser mon itinéraire
          </li>
        </ul>
      </div>
    </div>
    <div class="col-sm-9 col-sm-offset-3 col-xs-12 trip-home-content">
        <div class="trip-picture">
          <!-- TODO: set size of picture properly -->
          <%=  @trip.destination ? image_tag(@trip.destination.picture.url(:banner)) : image_tag('beachtrip.jpeg', height: 150, width: '100%') %>
        </div>
        <h1 class="text-center"><strong><%= @trip.title.upcase %></strong></h1>
        <div class="row">
          <div class="col-xs-6 text-center">
            <h3 class="text-center">
              <i class="fa fa-heart red"></i>
              <em> Expériences</em>
            </h3>
            <% if @trip.trip_experiences.any? %>
              <p class= 'light'>
                <%= @trip.trip_experiences.count %> expériences ajoutés au voyage
                <br>
                (dernier ajout: <%= "#{@trip.trip_experiences.last.name} le #{@trip.trip_experiences.last.created_at.strftime('%d-%m-%Y')}" %>)
              </p>
              <p class= 'light'>
                Ajouter plus d'expériences
              </p>
            <% else %>
              <p class= 'light'>
                 Tu n'as pas encore ajouté d'expériences à ton voyage...
              </p>
              <p class= 'light'>
                Clique ici pour y remédier!
              </p>
            <% end %>
          </div>
          <div class="col-xs-6 text-center">
            <h3 class="text-center">
              <i class="fa fa-comment fb-blue"></i>
              <em> Commentaires</em>
            </h3>
            <% if @trip.trip_comments.any? %>
              <p class= 'light'>
                <%= @trip.trip_comments.count %> expériences ajoutés au voyage
                <br>
                (dernier commentaire de <%= "#{@trip.trip_comments.last.name} le #{@trip.trip_comments.last.created_at.strftime('%d-%m-%Y')}" %>)
              </p>
              <p class= 'light'>
                Demander plus de conseils
              </p>
            <% else %>
              <p class= 'light'>
                 Tu n'as pas encore de conseils de tes amis...
              </p>
              <p class= 'light'>
                Clique ici pour leur demander!
              </p>
            <% end %>
          </div>
        </div>
    </div>
  </div>
</div>
